# Brain Routing Log

> Append-only log of the Brain's routing decisions. **Stage 2D (Learning)** reads this back (targeted grep + recency fallback) before each route/critique and lets past outcomes shape the present; when one does, it's recorded in the `recall:` field. One line per routing event. Fields: `candidates`/`chose`/`user-correction` (2A) · `fan-out`/`merge`/`failures` (2B) · `self-critique` (2C) · `recall` (2D) · `continuation` (2E).
> 備註(中):大腦每次路由的決定都記在這。一個決定一行。

(No routing events yet.)
