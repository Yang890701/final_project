# Brain Handoff Ticket (stage 2E — the across-session lane)

> The Brain's **live work-ticket**, distinct from PROJECT.md's slower checkpoint. When work pauses mid-flight, the Brain writes the next-session pickup here (open work · next concrete action · context pointers · done-criteria · in-flight fleet/scheduled job IDs). The next session's resume protocol reads this and continues **without the user re-explaining**. Transfer is asynchronous, via this file — not live remote-control. **Clear / archive a ticket once it's picked up and done** (a stale ticket mis-resumes the next session).
> 備註(中):大腦的「即時工單」,跟 PROJECT.md 的檢查點分開。工作中途暫停時,把下一棒要做的寫在這。下個 session 的 resume 流程讀它接力,你不用重講。接走做完就清掉。

---

## ACTIVE ticket

(none — no work is paused mid-flight.)

---

## ARCHIVED tickets

(none yet)
