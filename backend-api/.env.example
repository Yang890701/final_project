# 複製成 .env 並填值（.env 已被 .gitignore 忽略，勿入庫）
# 不填 DATABASE_URL → 自動 fallback 用 db/fixtures.json（可零基礎設施 demo）
DATABASE_URL=
# 不填 GEMINI_API_KEY → 偵測走規則 fallback（仍可 demo）
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
